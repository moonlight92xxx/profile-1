name = "syx-chat-proxy"
main = "index.js"
compatibility_date = "2024-01-01"

# Environment variables (secrets will be set via wrangler secret put)
[vars]
# GROQ_API_KEY should be set as a secret, not here:
# wrangler secret put GROQ_API_KEY
